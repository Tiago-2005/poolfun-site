* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: Arial, sans-serif;
}

/* Container centré */
.container {
    max-width: 1000px;
    margin: auto;
    padding: 30px;
    text-align: center;
}

/* Header */
header {
    background-color: #0288d1;
    color: white;
}

nav button {
    margin: 5px;
    padding: 10px 15px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

/* Pages */
.page {
    display: none;
    min-height: 80vh;
}

/* Fonds par rubrique */
.accueil { background-color: #e3f2fd; }
.categories { background-color: #f1f8e9; }
.tarifs { background-color: #fffde7; }
.equipe { background-color: #ede7f6; }
.contact { background-color: #e0f7fa; }
.avis { background-color: #fce4ec; }

/* Images */
img {
    width: 100%;
    max-width: 400px;
    border-radius: 10px;
    margin: 20px auto;
    display: block;
}

/* Cards */
.cards {
    display: flex;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
}

.card {
    background-color: white;
    padding: 15px;
    border-radius: 10px;
    width: 220px;
}

/* Formulaire */
form {
    max-width: 400px;
    margin: auto;
}

form input, form textarea {
    width: 100%;
    padding: 10px;
    margin-bottom: 10px;
}

form button {
    background-color: #0288d1;
    color: white;
    bo
